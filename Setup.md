Requirements.
    Node.js

Confirm node with the following commands:
    node --version
    npm --version

Install firebase:
    npm install -g firebase-tools
Verify:
    firebase --version
Login:
    firebase login

